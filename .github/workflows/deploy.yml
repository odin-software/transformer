name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: [self-hosted, tenma]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t localhost:5000/transformer:latest -t localhost:5000/transformer:${{ github.sha }} .

      - name: Push to local registry
        run: |
          docker push localhost:5000/transformer:latest
          docker push localhost:5000/transformer:${{ github.sha }}
